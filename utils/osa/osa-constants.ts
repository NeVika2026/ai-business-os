/** Client poll interval for live execution progress (ms). */
export const OSA_PROGRESS_POLL_INTERVAL_MS = 800;

/** Server-side control-check interval while execution is paused (ms). */
export const OSA_CONTROL_CHECK_INTERVAL_MS = 400;

/** Maximum OSA runs loaded on the workspace history panel. */
export const OSA_RUN_HISTORY_LIMIT = 20;
