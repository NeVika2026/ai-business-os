import { PlaceholderPage } from '@/components/layout/placeholder-page';

export default function WorkspacePage() {
  return (
    <PlaceholderPage
      title="Workspace"
      description="Unified workspace shell — connect modules and OSA execution context here."
    />
  );
}
